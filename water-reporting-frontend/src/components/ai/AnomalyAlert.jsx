import React from 'react';

const AnomalyAlert = () => {
    return (
        <div className="p-4 bg-md-error-container text-md-on-error-container rounded-xl flex items-center justify-center font-bold text-xs uppercase tracking-tighter">
            Anomaly Detection UI (Member B)
        </div>
    );
};

export default AnomalyAlert;
